const dbConfig = require(`./db-config.${process.env.NODE_ENV}.json`);

module.exports = {
  port: 9000,
  socketIoPort: 9001,
  postgres: dbConfig,
  tokenExpiresIn: '4h',
  offlineDelayTime: 2000,
};
Í